package Model;

import java.util.LinkedList;

public class PackAnimals extends Animals{

    public PackAnimals(Integer id, String name, String type, String birthDate, LinkedList<String> commands) {
        super(id, name, type, birthDate, commands);
    }
    
}
